import React from "react";
import Settings from "../../../components/app-modules/settings/Manage";

const page = () => {
  return (
    <div>
      <Settings />
    </div>
  );
};

export default page;
